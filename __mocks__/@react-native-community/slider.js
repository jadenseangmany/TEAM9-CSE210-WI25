// Sliders are also not working properly through jest
// ** GPT Generated Code

import React from 'react';
import { View } from 'react-native';

const Slider = (props) => <View {...props} />;

export default Slider;